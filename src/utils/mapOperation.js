//地图操作，包含按钮的点击功能
import Vue from 'vue'
window.func = window.func || {};
func.mapOperation = func.mapOperation || {};
func.mapOperation = {
    open:function () {
        //Vue.$store.dispatch('map/openDialogOfCar',true);
    }
};
export default func.mapOperation